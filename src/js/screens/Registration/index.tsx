import React from 'react';
import { RegBlock, Notification } from '../../components';

export const Registration = (props: any) => {
    return <>
        <RegBlock/>
        <Notification/>
    </>
};