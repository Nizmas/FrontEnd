import React from 'react';
import { AuthBlock, Notification } from '../../components';

export const Authorization = (props: any) => {
    return <>
        <AuthBlock/>
        <Notification/>
    </>
};